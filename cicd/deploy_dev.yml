deploy-test:
  extends:
    - .base_deploy
  tags:
    - delly
  environment:
    name: test/$BUILD_APP
  script:
    - ./run.bash $BUILD_APP 1 $REGISTRY_HOST/dellyliu/cicd-demo-$BUILD_APP:$CI_COMMIT_SHORT_SHA
  rules:
    - if: '$ENV == "test"'
